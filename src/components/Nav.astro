---
const links = [
  { label: 'About',      href: '#about' },
  { label: 'Membership', href: '#membership' },
  { label: 'Join',       href: '#join' },
];
---

<nav>
  <a class="nav-logo" href="#">Lounge 55</a>
  <ul class="nav-links">
    {links.map(({ label, href }) => (
      <li><a href={href}>{label}</a></li>
    ))}
  </ul>
  <div class="nav-actions">
    <a href="https://members.lounge55.club/#/portal/signup/6997a1bf8f563d0008e1a620/monthly" class="nav-join-btn">Join Now</a>
  </div>
  <button class="nav-mobile-toggle" aria-label="Open menu">&#9776;</button>
</nav>

<style>
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 18px 48px;
    background: linear-gradient(to bottom, rgba(10,10,10,0.97), transparent);
  }

  .nav-logo {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.6rem;
    letter-spacing: 0.12em;
    color: var(--orange);
    text-decoration: none;
  }

  .nav-links {
    display: flex;
    gap: 36px;
    list-style: none;
  }

  .nav-links a {
    font-family: 'Cinzel', serif;
    font-size: 0.68rem;
    letter-spacing: 0.2em;
    color: var(--text-muted);
    text-decoration: none;
    text-transform: uppercase;
    transition: color 0.3s;
  }

  .nav-links a:hover { color: var(--orange); }

  .nav-mobile-toggle {
    display: none;
    background: none;
    border: none;
    color: var(--orange);
    font-size: 1.4rem;
    cursor: pointer;
  }

  .nav-actions {
    display: flex;
    align-items: center;
    gap: 20px;
  }

  .nav-join-btn {
    font-family: 'Cinzel', serif;
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    text-decoration: none;
    color: var(--white);
    background: linear-gradient(135deg, var(--orange-bright), var(--orange-dark));
    padding: 10px 24px;
    clip-path: polygon(6px 0%, 100% 0%, calc(100% - 6px) 100%, 0% 100%);
    transition: all 0.3s;
  }
  .nav-join-btn:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 20px rgba(232,101,10,0.45);
  }

  @media (max-width: 800px) {
    nav { padding: 16px 20px; }
    .nav-links { display: none; }
    .nav-actions { gap: 14px; }
    .nav-mobile-toggle { display: block; }
  }
</style>
